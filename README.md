# News Scrapper FEU Social

## Run

Use the following command to start the application, recreating the database volume to avoid stale credential problems:

```bash
./run.sh
```

If you want to use your own database secrets, edit `.env` before running. If `.env` does not exist, the script will copy `.env.example` automatically.
